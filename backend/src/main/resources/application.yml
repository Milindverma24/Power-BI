spring:
  application:
    name: backend
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:aibi}
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:MySQL123}
    hikari:
      connection-timeout: 30000
      maximum-pool-size: 3
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: false
    properties:
      hibernate: {}
  flyway:
    enabled: true
    baseline-on-migrate: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID:your-google-client-id}
            client-secret: ${GOOGLE_CLIENT_SECRET:your-google-client-secret}
            scope:
              - email
              - profile

  mail:
    host: smtp.gmail.com
    port: 587
    username: jatinverma1234567890@gmail.com
    password: oupa dpog jalp srvn
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  port: ${SERVER_PORT:8080}
  forward-headers-strategy: framework

jwt:
  secret: ${JWT_SECRET:your-jwt-secret-key-that-is-very-long-at-least-256-bits}
  expiration: 86400000

app:
  frontend:
    url: ${FRONTEND_URL:http://localhost:5173}

langchain4j:
  gemini:
    chat:
      model:
        api-key: ${GEMINI_API_KEY:your-gemini-api-key}
        model-name: gemini-flash-latest
